#include "external/catch2/catch.hpp"
#include "core/Application.hpp"

using namespace Core;
TEST_CASE("Test for initializing the app", "[Core : Application]")
{
  // NO TESTS FOR NOW !
  REQUIRE(1 == 1);
}